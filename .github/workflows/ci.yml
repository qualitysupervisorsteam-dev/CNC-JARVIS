```yaml
name: CNC JARVIS CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

jobs:
  flutter:
    name: Flutter Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.0"
          channel: stable

      - name: Generate Flutter platform files
        shell: bash
        run: |
          flutter create \
            --platforms=android,windows \
            --org com.cncjarvis \
            --project-name cnc_jarvis \
            .

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

  android:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.0"
          channel: stable

      - name: Generate Android project
        shell: bash
        run: |
          flutter create \
            --platforms=android \
            --org com.cncjarvis \
            --project-name cnc_jarvis \
            .

      - name: Get dependencies
        run: flutter pub get

      - name: Build Android APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: cnc-jarvis-android
          path: build/app/outputs/flutter-apk/app-release.apk

  windows:
    name: Windows Build
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.0"
          channel: stable

      - name: Generate Windows project
        shell: pwsh
        run: |
          flutter create `
            --platforms=windows `
            --org com.cncjarvis `
            --project-name cnc_jarvis `
            .

      - name: Get dependencies
        run: flutter pub get

      - name: Build Windows
        run: flutter build windows --release

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: cnc-jarvis-windows
          path: build/windows/x64/runner/Release
```
